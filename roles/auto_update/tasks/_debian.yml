---
- name: Execute following tasks with sudo
  become: true
  block:
    - name: Install Auto Update and Auto Upgrade
      ansible.builtin.apt:
        name:
          - unattended-upgrade  # auto install of updates
          - apt-config-auto-update  # auto update package cache
